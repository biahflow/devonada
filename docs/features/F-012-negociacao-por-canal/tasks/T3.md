# T3 — O resultado da negociação vira dado, inclusive quando não houve acordo

## Identity

```text
feature_id: F-012
task_id: T3
parent_plan: docs/features/F-012-negociacao-por-canal/plan.md
depends_on: none
```

## Goal

`orm.Renegociacao` é grava-e-esquece: escrita num ponto só (`parcelas.py:174-185`), **sem nenhum
`GET` que a devolva**, e só nasce quando houve acordo. Recusa, contraproposta e silêncio do credor
são metade da informação que constrói o benchmark, e hoje são jogados fora. Esta tarefa cria a
entidade que os comporta e a rota de leitura que hoje não existe.

## Scope

- `backend/orm.py` — `ResultadoNegociacao` (nova): `tenant_id`, `divida_id`, `canal`, `desfecho`
  (`acordo` · `recusa` · `contraproposta` · `sem_resposta`), `valor_proposto` e `valor_obtido`
  opcionais, `renegociacao_id` opcional, `observacao` opcional, `registrado_em`. Append-only, no
  molde de `Renegociacao` (96-112).
- `backend/alembic/versions/` — UMA migração forward-only encadeada em `116f2181bdda`, no estilo de
  `116f2181bdda_marco_unique_tenant_tipo.py`, com índice em `tenant_id`.
- `backend/schemas.py` — request e response do registro e da leitura.
- `backend/routers/` — `POST /v1/dividas/{id}/negociacoes` → 201;
  `GET /v1/dividas/{id}/negociacoes` e `GET /v1/negociacoes` (do tenant, que é o que constrói o
  benchmark).
- `backend/tests/` — grava e lê de volta, os quatro desfechos, o isolamento por tenant.

## Out of Scope

- **Afrouxar os `NOT NULL` de `orm.Renegociacao`.** A ADR 0021, item 6, recusou explicitamente:
  mexeria em dado em produção e misturaria o que aconteceu na conversa com o que mudou no contrato.
  `Renegociacao` continua sendo só o acordo.
- **Mexer no marco.** `primeira_negociacao` continua nascendo do acordo fechado, ao lado do INSERT
  que o produz (`parcelas.py:190` chamando `registrar_marcos`), e continua não se desfazendo.
  Registrar um resultado **não** dispara marco.
- **Benchmark agregado entre usuários** — média de mercado, "credores que mais dão desconto",
  comparação social. Este contrato coleta e devolve o dado do **próprio tenant**.
- Qualquer arquivo em `src/`: **T5**.
- Script e canal na leitura: T1 e T2.
- **Não editar `backend/routers/conta.py`** — a varredura é derivada do metadata.

## Acceptance Criteria

- **T3-AC1** — o resultado é gravado com canal e desfecho, e devolvido por rota de leitura. Há teste
  que grava e lê de volta.
- **T3-AC2** — os quatro desfechos são aceitos, inclusive os três **sem** acordo.
- **T3-AC3** — `desfecho: acordo` aceita `renegociacao_id`, e os demais o deixam nulo.
- **T3-AC4** — a tabela nova tem `tenant_id`, e um teste **prova** que ela aparece em
  `tabelas_do_tenant()` sem ninguém ter editado `routers/conta.py`.
- **T3-AC5** — o marco `primeira_negociacao` continua sendo disparado pelo acordo em
  `parcelas.py:190` e **não** pelo registro de resultado; a regressão do M11 segue verde.
- **T3-AC6** — recurso de outro tenant devolve **404, nunca 403**.
- **T3-AC7** — `alembic upgrade head`, `downgrade -1` e `upgrade head` rodam contra banco limpo, com
  o DDL conferido contra `CreateTable(Base.metadata)`.
- **T3-AC8** — `cd backend && venv/bin/pytest` passa inteiro.

## Validation

```text
baseline: cd backend && venv/bin/pytest
          → 620 passed, 23 warnings (20/08/2026). Nenhuma falha preexistente.

required: pytest:  cd backend && venv/bin/pytest
          alembic: cd backend && venv/bin/alembic upgrade head
                   cd backend && venv/bin/alembic downgrade -1
                   cd backend && venv/bin/alembic upgrade head
```

## Required Capabilities

```text
READ:     repositório inteiro
WRITE:    backend/orm.py · backend/alembic/versions/ · backend/schemas.py · backend/routers/ ·
          backend/tests/
VALIDATE: pytest, alembic
COMMIT:   allowed — um commit
```

## Context to Read First

- `backend/orm.py:96-112` — `Renegociacao`, o molde da tabela append-only.
- `backend/routers/parcelas.py:145-210` — a rota de renegociação inteira, incluindo o INSERT
  (174-185) e o `registrar_marcos` (190).
- `backend/routers/conta.py:20-32` — `tabelas_do_tenant()`, derivada do metadata.
- `docs/adr/0021-...md`, itens 6 e 7.
- `backend/alembic/versions/116f2181bdda_marco_unique_tenant_tipo.py` — o estilo de migração.
- **Atenção (PF-5 do plano):** `registrar_marcos` **não** está em `domain/marcos.py`, como o contrato
  sugere. Está em `backend/routers/marcos.py:47-98`; o que está em `domain/marcos.py:53-92` é
  `marcos_atingidos`, função pura. Você não precisa tocar em nenhum dos dois.

## Known Risks

- **"Aproveitar" e mexer na `Renegociacao` existente.** Proibido pelo contrato e pela ADR.
- **`conftest.py` monta o schema com `Base.metadata.create_all`, não pelo Alembic.** Uma migração
  divergente do ORM passa a suíte inteira verde. A conferência de T3-AC7 é manual e obrigatória.
- **Disparar o marco no registro de resultado.** Parece natural e está errado: o marco é do acordo,
  e duplicar o gatilho o tornaria atingível sem que nada tenha sido acordado.
- Escrever a coluna `canal` como texto livre. Ela é enumerada; texto livre é exatamente o que o
  `observacao` já faz hoje e o que esta feature existe para substituir.

## Human Gates

- Modelagem do resultado de negociação — **SATISFEITO** pela ADR 0021, item 6 (entidade nova).
- Migração nova. Aditiva, sem dado migrado.

## Reporting

Termine com o `BUILD REPORT` completo. **Grave-o também em
`docs/features/F-012-negociacao-por-canal/reports/T3.md`**, versionado neste commit.
