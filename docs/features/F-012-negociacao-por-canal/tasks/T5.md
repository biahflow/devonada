# T5 — O canal deixa de ser placeholder de texto livre e vira dado

## Identity

```text
feature_id: F-012
task_id: T5
parent_plan: docs/features/F-012-negociacao-por-canal/plan.md
depends_on: T3
```

## Goal

Hoje o canal só aparece como **placeholder** do campo `observacao` na tela de renegociar
(`app/(tabs)/dividas/[id]/renegociar.tsx:125`, *"Acordo por telefone, protocolo 12345"*). Texto livre
não sustenta benchmark nenhum. Esta tarefa dá campos tipados ao canal e ao desfecho — e faz caber a
recusa, a contraproposta e o silêncio, que hoje não são registráveis em lugar nenhum.

## Scope

- `src/api/types.ts`, `src/api/` e `src/hooks/` — o tipo e as chamadas de resultado de negociação.
- `app/(tabs)/dividas/[id]/renegociar.tsx` — canal e desfecho como campos **tipados**.
- A tela de registro cobre os quatro desfechos, inclusive os três sem acordo: registrar uma recusa
  **não pode exigir** preencher valor de acordo.
- A leitura do que foi registrado, para a dívida.
- `src/test/screens/` — os quatro estados e os quatro desfechos.

## Out of Scope

- **Telas de benchmark agregado entre usuários**, "credores que mais dão desconto" ou comparação
  social. Fora de escopo pelo contrato: agregar entre tenants é decisão de produto e de privacidade
  que ninguém tomou.
- **Mudar o fluxo de renegociação que já existe.** Registrar acordo continua chamando
  `POST /v1/dividas/{id}/renegociacao` e reescrevendo as parcelas. O resultado é registro
  **adicional**, não substituto.
- `ScriptCard` e canal na leitura do script: **T4**.
- Calcular desconto, percentual obtido ou qualquer derivado no cliente.
- Backend.

## Acceptance Criteria

- **T5-AC1** — canal e desfecho são campos tipados, não texto livre.
- **T5-AC2** — os quatro desfechos são registráveis, e registrar recusa ou silêncio **não** exige
  valor de acordo.
- **T5-AC3** — o fluxo de renegociação existente continua funcionando sem mudança de comportamento.
- **T5-AC4** — os quatro estados de tela têm teste.
- **T5-AC5** — 48pt e `accessibilityLabel` nos controles novos.
- **T5-AC6** — nenhum valor é calculado no cliente.
- **T5-AC7** — os seis gates do front passam.

## Validation

```text
baseline: npx jest → 45 suítes / 539 testes (20/08/2026). Meça de novo antes de começar.
          Se T2 já tiver entrado, o typecheck do front está reprovando por causa do campo `script`;
          isso é escopo de T4, não seu. Registre como baseline e não conserte.

required: npm run typecheck ; npm run lint ; npm test ; npm run bundle:check ;
          npm run palette:check ; npm run digits:check
```

## Required Capabilities

```text
READ:     repositório inteiro
WRITE:    src/api/types.ts · src/api/ · src/hooks/ · app/(tabs)/dividas/[id]/renegociar.tsx ·
          src/test/screens/
VALIDATE: npm scripts
COMMIT:   allowed — um commit
```

## Context to Read First

- `app/(tabs)/dividas/[id]/renegociar.tsx` inteiro, e em especial o `FormField` de observação que
  começa na linha 120.
- `src/hooks/useParcelas.ts:81` — a mutação de renegociação que já existe.
- `docs/adr/0021-...md`, itens 6 e 7 — o canal persiste no resultado, não na leitura.
- `docs/domain.md:316-336` — os rótulos corretos dos três canais.
- `docs/guardrails.md`, seção 4 — tom anti-ansiedade. Registrar uma recusa não pode soar como
  registrar um fracasso.

## Known Risks

- **Exigir valor para registrar recusa.** É o viés que o contrato nomeia: "o dado de benchmark nasce
  enviesado se só o acordo fechado for registrado". Um formulário que obriga valor recria o
  problema que a entidade nova resolveu.
- **Duplicar o registro.** Fechar um acordo já grava `Renegociacao`; o resultado é adicional. Deixe
  claro na tela que o usuário não está registrando duas vezes a mesma coisa.
- Divide `src/api/types.ts` com T4.

## Human Gates

- Validação em device. Nenhum agente a declara satisfeita.

## Reporting

Termine com o `BUILD REPORT` completo, e diga o que não foi validado em device. **Grave-o também em
`docs/features/F-012-negociacao-por-canal/reports/T5.md`**, versionado neste commit.
