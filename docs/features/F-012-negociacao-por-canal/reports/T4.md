# BUILD REPORT — F-012 T4

```text
Status: BUILD_COMPLETE
Files changed:
  - src/api/types.ts — `Canal`, `MomentoScript`, `BlocoScript`, `ScriptNegociacao`
    (novos). `RevisaoCobranca.script` de `string | null` para `ScriptNegociacao`
    (não nulável); `ValorJustoCardData.script` de `string` para `ScriptNegociacao`.
  - src/api/revisao.ts — `obterRevisao(dividaId, canal='email')` manda `?canal=`.
  - src/hooks/useRevisao.ts — a chave de cache inclui o canal; `placeholderData:
    keepPreviousData` para trocar de canal não piscar a tela inteira.
  - src/components/cards/ScriptCard.tsx (novo) — seletor de canal (OptionGroup),
    blocos separados por `momento`, borda verde de 2px no argumento, selo de
    segurança no alerta/regra, botão copiar POR BLOCO nos canais escritos.
  - app/(tabs)/dividas/[id]/revisao.tsx — passa a exibir o ScriptCard SEMPRE,
    inclusive sem achado; o antigo bloco de script inline saiu.
  - src/components/cards/ValorJustoCard.tsx — copia o script juntando os blocos
    (mantém o copiar único do chat, sem mudar o que exibe).
  - src/test/screens/revisao.test.tsx — reescrito: quatro estados, os três canais,
    o caso sem achado, copiar por bloco.
  - src/test/screens/triagem.test.tsx — mock de `RevisaoCobranca` ganha o `script`
    (agora obrigatório); a triagem não o exibe.
  - docs/features/F-012-negociacao-por-canal/reports/T4.md (novo) — este relatório.

Validation executed (os seis gates do front, todos VERDES):
  - npm run typecheck → sem erros.
  - npm run lint → sem erros.
  - npm test → 46 suites / 545 testes, todos passam. (O Jest não encerra o
    processo ~1s após o resumo; é preexistente, não é falha.)
  - npm run bundle:check → exporta o bundle iOS (4,77 MB), grafo inteiro compila.
  - npm run palette:check → exit 0.
  - npm run digits:check → exit 0.

Baseline: antes de T4, o typecheck do front REPROVAVA por causa da mudança de
forma do campo `script` que T2 fez no backend — esperado e registrado no Task
Contract. Consertá-lo era o trabalho desta tarefa.

Validation skipped: none (dos gates automatizáveis).

NÃO validado em device (gate humano, ABERTO):
  - Leitura dos blocos, separação visual entre segurança e contestação, botão de
    copiar por bloco, seletor de canal, alvo de toque de 48pt PERCEBIDO, safe area
    e teclado — nada disso foi visto em aparelho. Um agente não consegue, e não
    afirma que fez. Os gates provam que a tela renderiza, reage, que a copy não
    escorrega e que os pares de cor passam o piso — piso, não legibilidade.

Assumptions:
  - Os dois blocos de segurança (`alerta-validacao`, `regra-pagamento`) recebem
    tratamento visual próprio (selo + tom recuado), separando-os do argumento —
    é o risco nomeado no contrato: texto de segurança e de contestação mal
    separados leem como "a dívida tem problema".
  - `keepPreviousData` no hook: sem ele, trocar de canal jogaria a tela em
    "carregando" e o seletor sumiria — o pior tipo de bug de tela. A chave por
    canal (T4-AC2) garante que cada variante tem seu próprio cache.
  - Copiar por bloco só nos canais escritos (`copiavel=true`); no telefone o
    script é guia de fala e não leva botão de copiar (herdado da decisão de T1).
  - 48pt garantido por estilo (`minHeight: 48`) no seletor (OptionGroup) e no
    botão de copiar; a acessibilidade por `accessibilityLabel` é exercitada pelo
    teste (`findByLabelText`). A PERCEPÇÃO do alvo é gate de device.

Remaining risks:
  - Copy de negociação nas três variantes ainda não passou por advogado — gate de
    pré-lançamento aberto, não declarado satisfeito.
  - A separação visual entre segurança e contestação foi desenhada por tokens,
    mas só o device confirma que ela lê como separação, e não como um card só.

Human decisions required:
  - Validar em device o ScriptCard com seletor de canal (gate humano aberto).
```

## Desvios conscientes do spec

`src/test/screens/triagem.test.tsx` está fora dos `expected_areas`, mas seu mock de
`RevisaoCobranca` quebrava o typecheck com o campo `script` agora obrigatório — atualizar é
consequência direta da mudança de contrato, não trabalho de outra área. A tela de triagem em si
não foi tocada.

## Oportunidades vistas e NÃO implementadas (fora de escopo)

- Registro de resultado de negociação na tela de renegociar — T5.
- O verbete de design system do `ScriptCard` (promoção de "Ainda só especificação") — T6.
- Alinhar o regex de copy do front com o do backend — T6, PF-4.
