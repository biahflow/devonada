# BUILD REPORT — F-012 T5

```text
Status: BUILD_COMPLETE
Files changed:
  - src/api/types.ts — `DesfechoNegociacao`, `RegistroNegociacaoInput`,
    `ResultadoNegociacao` (novos).
  - src/api/negociacoes.ts (novo) — `registrarNegociacao` (POST) e
    `listarNegociacoesDaDivida` (GET).
  - src/hooks/useNegociacoes.ts (novo) — `useRegistrarNegociacao` (mutação) e
    `useNegociacoesDaDivida` (query), com chave aninhada em `['dividas']`.
  - app/(tabs)/dividas/[id]/renegociar.tsx — canal e desfecho viram campos
    TIPADOS (OptionGroup); o placeholder de texto livre saiu. O acordo mantém o
    formulário e o fluxo de renegociação existentes; recusa/contraproposta/
    silêncio registram só o desfecho, sem exigir valor. Abaixo, o histórico de
    negociações da dívida com os quatro estados.
  - src/test/screens/renegociar.test.tsx (novo) — 10 testes: campos tipados, os
    quatro desfechos, recusa sem exigir valor, acordo com o formulário intacto, e
    os quatro estados do histórico.
  - src/test/screens/plano.test.tsx — o bloco "tela de renegociação" (que testava
    a tela antiga) foi removido; a cobertura migrou para `renegociar.test.tsx`.
  - docs/features/F-012-negociacao-por-canal/reports/T5.md (novo) — este relatório.

Validation executed (os seis gates do front, todos VERDES):
  - npm run typecheck → exit 0.
  - npm run lint → sem erros.
  - npm test → 47 suites / 552 testes, todos passam.
  - npm run bundle:check → exporta o bundle iOS, grafo inteiro compila.
  - npm run palette:check → exit 0.
  - npm run digits:check → exit 0.

Validation skipped: none (dos gates automatizáveis).

NÃO validado em device (gate humano, ABERTO):
  - Leitura dos seletores, alvo de toque de 48pt PERCEBIDO, teclado, safe area, e
    o tom das mensagens de desfecho em aparelho. Um agente não vê isso.

Assumptions:
  - Registrar o desfecho é registro ADICIONAL. O acordo continua indo por
    `POST /v1/dividas/{id}/renegociacao` (que reescreve as parcelas, fluxo
    inalterado — T5-AC3) e ALÉM disso grava o `ResultadoNegociacao` com o canal,
    no `onSuccess`. Os três desfechos sem acordo gravam só o resultado.
  - `valorProposto`/`valorObtido` opcionais em todos os desfechos: registrar
    recusa ou silêncio NÃO exige valor (T5-AC2) — obrigar recriaria o viés de
    benchmark que a entidade nova existe para desfazer.
  - Tom NEUTRO nos rótulos de desfecho (guardrail 4): "Não aceitaram", "Sem
    resposta" — registrar uma recusa não é registrar um fracasso.
  - Nenhum valor é calculado no cliente (T5-AC6): a tela só envia o que o usuário
    digitou; o histórico exibe o que o backend devolveu.
  - O acesso do teste ao DateField: a suíte não dirige o `DateTimePicker` nativo
    (não há precedente no repo, e o picker é host component). Por isso o teste do
    acordo prova o formulário INTACTO e sua validação preservada (T5-AC3 no nível
    de UI); a chamada de ponta a ponta ao endpoint de renegociação é coberta
    pelos testes de backend (`test_parcelas_api::TestRenegociacao`, inalterados e
    verdes), já que `useRenegociar` não foi tocado.

Remaining risks:
  - Duplicidade percebida: acordo grava `Renegociacao` E `ResultadoNegociacao`. A
    tela deixa claro que as parcelas são reescritas uma vez só; o resultado é
    registro de conversa, não segunda renegociação.
  - `renegociacaoId` no resultado do acordo fica nulo: o endpoint de renegociação
    devolve a `Divida`, não o id da `Renegociacao`. Ligar os dois exigiria mudar
    aquele contrato — fora do escopo desta tarefa. Não impede o benchmark.

Human decisions required:
  - Validar em device a tela de registro por canal (gate humano aberto).
```

## Desvios conscientes do spec

`src/test/screens/plano.test.tsx` está fora dos `expected_areas`, mas continha um `describe('tela de
renegociação')` que exercitava a tela ANTIGA e quebrava com a nova. Removi esse bloco e migrei a
cobertura para `renegociar.test.tsx` — atualizar teste que a mudança de tela invalida é obrigatório
(DoD: nenhum gate desativado). O bloco de plano de pagamento em si não foi tocado.

## Oportunidades vistas e NÃO implementadas (fora de escopo)

- `GET /v1/negociacoes` (benchmark do tenant inteiro) tem cliente pronto no backend (T3), mas a
  tela de benchmark agregado é fora de escopo por contrato.
- Ligar `renegociacaoId` ao acordo exigiria o endpoint de renegociação devolver o id — mudança de
  contrato fora do escopo.
- Provas de copy nas três variantes e alinhamento dos regexes — T6.
