# BUILD REPORT — F-012 T6

```text
Status: BUILD_COMPLETE
Files changed:
  - src/test/screens/negociacao-copy.test.tsx (novo) — varredura de copy das TRÊS
    variantes de canal do ScriptCard, com PROVA POR INJEÇÃO (termo proibido
    plantado em cada variante faz a varredura falhar). 6 testes.
  - backend/tests/test_caixa_integracao.py — `TestScriptDeNegociacao` ganha a
    regressão PF-3: o valor recitado pelo script sai do MESMO motor
    (`capacidade_atual`) que o simulador lê.
  - docs/api-contract.md — seção 3.15 (script por canal + resultado de
    negociação), Bloco 15 na fila, e a M6 apontando para a forma nova de `script`.
  - docs/design-system.md — `ScriptCard` promovido de "Ainda só especificação"
    para a seção dos componentes que existem, escrito do que T4 entregou.
  - docs/guardrails.md — seção 3.1 (validação de canal / anti-golpe), nova
    (PF-7); a regra de copy passa a citar os três canais e o conjunto alinhado.
  - roadmap.md — os itens de F-012 no M12 marcados `[~]`, e a mudança de
    comportamento em produção declarada (a oferta sai do primeiro contato escrito).
  - docs/inventario.md e docs/engineering-os-adoption.md — baseline novo, MEDIDO.
  - docs/features/F-012-negociacao-por-canal/evidence.md (novo) — os seis Build
    Reports referenciados, com atribuição por tarefa.
  - docs/features/F-012-negociacao-por-canal/reports/T6.md (novo) — este relatório.

  PF-4 (alinhamento dos dois regexes): o regex do front já fora alinhado ao do
  backend em T4 (`src/test/screens/revisao.test.tsx` ganhou `nul[ao]\b`); T6
  confirma que os dois lados varrem o MESMO conjunto de termos e adiciona a
  varredura das três variantes de canal nos dois. As duas TÉCNICAS ficam: backend
  campo a campo do `Achado` e dos blocos de `montar_script`; front sobre a árvore
  renderizada via `JSON.stringify`.

Validation executed (os SETE perfis, todos VERDES):
  - npm run typecheck → exit 0.
  - npm run lint → sem erros.
  - npm test → 48 suites / 558 testes passam.
  - npm run bundle:check → bundle iOS exportado.
  - npm run palette:check → exit 0.
  - npm run digits:check → exit 0.
  - cd backend && venv/bin/pytest → 685 testes, 23 avisos, verdes.

Baseline MEDIDA no início (não copiada): pytest 662, jest 45/539. FINAL: pytest
685 (+23 líquidos em T6: PF-3 e as varreduras; o resto veio de T2–T5), jest 48/558.

Validation skipped: none dos gates automatizáveis.

Cobertura dos ACs de T6:
  - T6-AC1 → negociacao-copy.test.tsx: prova por injeção nas três variantes.
  - T6-AC2 → os dois regexes = mesmo conjunto (alinhado em T4, confirmado aqui).
  - T6-AC3 → test_revisao_api.py::TestCanal.test_script_sem_achado_nao_afirma_nada
    (planta dívida sem extracao_id, alerta presente e nenhuma afirmação de valor).
  - T6-AC4 → test_revisao_api.py::TestCanal.test_os_tres_canais_tem_o_mesmo_valor
    _justo_e_os_mesmos_achados.
  - T6-AC5 → test_negociacoes_api.py::TestNaoDisparaMarco (registro não dispara) +
    test_renegociacao_de_verdade_continua_disparando_o_marco.
  - T6-AC6 → design-system.md: ScriptCard fora de "Ainda só especificação".
  - T6-AC7 → roadmap.md: "Mudança de comportamento a declarar".
  - T6-AC8 → evidence.md referencia os seis reports/ com atribuição.
  - T6-AC9 → os sete perfis verdes; executado × pulado distinguidos aqui.

NÃO validado / gates humanos ABERTOS:
  - Revisão da copy das três variantes por advogado — NÃO declarada satisfeita
    (proibido a T6). Teste de copy pega palavra proibida; não avalia postura
    jurídica.
  - Validação em device do ScriptCard e da tela de registro — não vista em
    aparelho.
  - Round-trip da migração contra Postgres — Postgres indisponível; feito contra
    SQLite.

Assumptions:
  - PF-3 reinterpretado: a oferta recitada (o que se pode comprometer NESTE acordo)
    e o aporte que o simulador ACEITA (extra sobre as parcelas atuais) são números
    diferentes por construção — o primeiro substitui a parcela desta dívida, o
    segundo não. A regressão prova o que PF-3 realmente pede: os dois saem do MESMO
    `capacidade_atual`, então quando o F-011 mudar a capacidade, os dois se movem
    juntos. Com uma dívida só, a oferta = `capacidade_hoje` daquela leitura.
  - Baseline registrada como `main + F-012` (a worktree ramificou antes do F-011).

Remaining risks:
  - Merge com F-011 pode conflitar em `backend/schemas.py`, `src/api/types.ts` e
    nos docs derivados (baseline, api-contract) — previsto no PARALLELISM_RISK.

Human decisions required:
  - Revisão de copy por advogado (pré-lançamento); validação em device; round-trip
    contra Postgres; e a passagem para DONE.
```

## Desvios conscientes do spec

Nenhum desvio de escopo. O único ajuste de leitura foi o PF-3 (ver Assumptions): a formulação
literal "o valor recitado é o mesmo que o simulador aceita" é impossível ao pé da letra, porque a
oferta e o aporte têm semânticas diferentes; a regressão prova o que a intenção pede — mesmo motor,
mesma queda quando o F-011 mudar a capacidade.

## Oportunidades vistas e NÃO implementadas (fora de escopo)

- Tela de benchmark agregado (`GET /v1/negociacoes` já tem cliente no backend) — fora de escopo por
  contrato (privacidade entre tenants).
- Ligar `renegociacaoId` ao acordo — exigiria mudar o contrato do endpoint de renegociação.
