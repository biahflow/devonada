# BUILD REPORT — F-012 T1

```text
Status: BUILD_COMPLETE
Files changed:
  - backend/domain/script.py (novo) — módulo puro `montar_script`, `BlocoScript`, `Canal`,
    substituindo a `montar_script` de string única de `routers/revisao.py:114-157` (a
    substituição do consumidor em si é escopo de T2, não desta tarefa).
  - backend/tests/test_script.py (novo) — 17 testes cobrindo T1-AC1 a T1-AC6.
  - docs/features/F-012-negociacao-por-canal/reports/T1.md (novo) — este relatório.

Validation executed:
  - cd backend && venv/bin/pytest → 637 passed, 23 warnings (baseline 620 + 17 novos deste
    módulo). Nenhuma falha. Nenhum teste que passava parou de passar. Os 23 avisos são os
    mesmos da baseline (HTTP_422_UNPROCESSABLE_ENTITY depreciado, Starlette/httpx,
    InsecureKeyLength) — nenhum novo.
  - cd backend && venv/bin/pytest tests/test_script.py -v → 17 passed isoladamente, antes da
    suíte completa.

Validation skipped: none

Unavailable capabilities: none

Assumptions:
  - Interpretação de escopo canal × segurança, decidida por leitura literal de
    `docs/domain.md:316-336`: "Duas regras valem para os canais escritos, e elas são de
    segurança, não de estilo" — o verbete escopa `_ALERTA_VALIDACAO_CANAL` e
    `_REGRA_PAGAMENTO` a `chat` e `email`, EXCLUINDO `telefone`. A mesma frase aparece
    verbatim, com o qualificador "escrito", três vezes no Task Contract (Scope, Goal, T1-AC3).
    `telefone` continua nunca devolvendo `None` (script mínimo de segurança vale para os três
    canais), mas seu script sem achado não contém as duas constantes — é guia de fala para um
    número que o próprio usuário discou, não mensagem copiável. Documentado no docstring do
    módulo com a citação da fonte. T1-AC4 ("dívida sem achado... contendo alerta e regra de
    pagamento") foi testado nos dois canais escritos, que é onde o próprio domain.md e o
    Scope colocam as duas constantes; há teste equivalente para `telefone` confirmando que ele
    também nunca devolve `None`/vazio e não contém bloco de `argumento`.
  - `capacidade_mensal` continua replicando a regra de `_capacidade_para_oferta` de
    `revisao.py`: só vira bloco de oferta quando `not None and > 0`. Testado com valor
    negativo (`test_capacidade_negativa_nao_vira_oferta`).
  - Posicionamento da oferta (ADR 0021, item 5) foi implementado como: `telefone` — bloco
    `momento="oferta"` inserido no fluxo normal, antes do bloco de fechamento (mesma posição
    relativa do `linhas.insert(-1, ...)` de hoje); `chat`/`email` — bloco separado, com
    `titulo` explicitando "depois que o credor responder" / "segundo e-mail — depois da
    resposta", posicionado após o pedido de demonstrativo e antes da regra de pagamento (que
    continua fechando o script). Como a ADR não prescreve estrutura de dados, a distinção
    testável escolhida foi a marcação textual de "usar depois" mais a ausência de oferta no
    bloco de abertura — critério explícito em T1-AC2 e coberto por um teste por canal.
  - `BlocoScript.titulo` e `id` de cada bloco são valores estáveis e legíveis
    (`alerta-validacao`, `saudacao`, `argumento-{achado.id}`, `oferta`,
    `pedido-demonstrativo`, `regra-pagamento`) — não especificados pelo contrato além do tipo
    (`str | None`), escolhidos para servir de chave estável a T2/T4 sem exigir nova decisão.

Remaining risks:
  - Copy de negociação nova (saudação, pedido de demonstrativo, alerta, regra de pagamento,
    três variantes de oferta) ainda não passou por revisão de advogado — gate de
    pré-lançamento explicitamente aberto no plano, e nenhuma tarefa o declara satisfeito,
    inclusive esta.
  - `test_script.py::TestCopyNasTresVariantes` usa o MESMO regex de
    `test_revisao.py::TestCopy` (não o regex do front, ainda divergente — PF-4, atribuído a
    T6). Alinhar os dois regexes do repositório continua fora do escopo desta tarefa.
  - A leitura de "telefone não leva as duas constantes de segurança" é uma interpretação da
    especificação (ver Assumptions); se a intenção real fosse "todo canal, inclusive
    telefone", o ajuste é local a `montar_script` (adicionar os dois blocos também para
    telefone) e aos dois testes que afirmam o contrário — não exige redesenho.

Human decisions required:
  - Confirmar (ou corrigir) a interpretação de escopo "telefone não leva as duas constantes
    de segurança", registrada acima, antes de T2 consumir este módulo — a rota herda
    diretamente o que `montar_script` decide.
  - Gate de pré-lançamento de revisão de copy por advogado (já registrado no plano/roadmap;
    esta tarefa amplia a superfície que ele precisa cobrir, sem declará-lo satisfeito).
```

## Desvios conscientes do spec

Nenhum desvio de escopo, arquivo ou comportamento do que o Task Contract pede. O único ponto
que exigiu decisão de leitura (não desvio) foi o escopo de `telefone` × as duas constantes de
segurança, registrado em `Assumptions` acima, com a citação literal de `docs/domain.md:316-336`
e do próprio Task Contract que sustenta a leitura.

## Oportunidades vistas e NÃO implementadas (fora de escopo)

- Substituir `routers/revisao.py::montar_script` pela chamada a este módulo — T2.
- Qualquer schema Pydantic ou mudança de rota — T2.
- Alinhar o regex de copy do front (`src/test/screens/revisao.test.tsx:150`) com o do backend
  — T6, PF-4.
- Qualquer mudança em `backend/domain/revisao.py`, seus achados ou a soma do `valorJusto` — não
  tocado, conforme Out of Scope.
- Nenhum arquivo de `backend/domain/caixa.py`, `backend/orm.py`, `backend/leitura.py` ou
  `backend/tests/test_caixa.py` foi tocado — são escopo da F-011 T1, em execução paralela no
  mesmo worktree.
