# BUILD REPORT — F-012 T2

```text
Status: BUILD_COMPLETE
Files changed:
  - backend/routers/revisao.py — a `montar_script` de string única (114-157) foi
    REMOVIDA e substituída por `_montar_script`, que só converte os blocos
    tipados de `domain/script.montar_script` (T1) para o contrato. `revisar_divida`
    passa a receber `canal` (default `email`); a rota ganha
    `?canal=telefone|chat|email`. Import de `formatar_brl` removido (só a função
    velha o usava); import de `domain.script.montar_script` acrescentado.
  - backend/schemas.py — `MomentoScript`, `BlocoScript`, `ScriptNegociacao`
    (novos). `RevisaoCobranca.script` passa de `str | None` para `ScriptNegociacao`
    (não nulável — `montar_script` deixou de devolver `None`); `ValorJustoCard.
    script` de `str` para `ScriptNegociacao`. MUDANÇA DE CONTRATO declarada, não
    aditiva.
  - backend/tests/test_revisao_api.py — `TestCanal` (nova: 7 testes) e helper
    `_textos`; dois testes de `TestRota` atualizados para a forma nova.
  - backend/tests/test_revisao.py — `TestCopy` ganha a varredura das TRÊS
    variantes de canal (T2-AC8).
  - backend/tests/test_caixa_integracao.py — `TestScriptDeNegociacao`: as quatro
    asserções de oferta passam a ler os blocos do script (a forma mudou; o valor
    e a frase continuam presentes no canal default email).
  - docs/features/F-012-negociacao-por-canal/reports/T2.md (novo) — este relatório.

  NÃO tocado: backend/routers/chat.py. O filtro da linha 137
  (`if r.valorJusto is None or not r.script: continue`) fica LITERALMENTE como
  está (PF-2). Com `script` agora sempre presente (objeto, truthy), a condição
  operante é `valorJusto is None` — exatamente o comportamento desejado: sem
  achado com valor, o card não sai (ADR 0008). O card já herda os blocos do canal
  default porque `revisar_divida` tem `canal="email"` por default. Nenhuma linha
  foi necessária em chat.py.

Validation executed:
  - cd backend && venv/bin/pytest → 684 passed, 23 warnings (baseline pós-T3 =
    676; +8 líquidos: 7 em TestCanal + 1 varredura de copy; dois testes existentes
    foram ATUALIZADOS, não somados). Nenhuma falha.

Validation skipped:
  - Gates do front (typecheck/lint/jest/etc.): esta tarefa é backend-only. A
    mudança de forma do campo `script` QUEBRA o typecheck do front de propósito —
    `src/api/types.ts` ainda declara `script: string | null`. Consertar o front é
    T4; declarar a quebra aqui é o combinado do plano.

Unavailable capabilities: none

Assumptions:
  - `email` como default da rota (PF-1): o texto único de antes já era mensagem
    formal e estruturada. Declarado no código; T6 o declara no api-contract.md.
  - `RevisaoCobranca.script` ficou NÃO NULÁVEL: `montar_script` sempre devolve ao
    menos saudação + pedido (e, nos escritos, alerta + regra). Truthful e é o que
    faz o filtro do chat reduzir a `valorJusto is None` sem afrouxar nada.
  - O canal NÃO persiste (ADR 0021, item 7): `test_o_canal_nao_e_gravado` prova
    que ler revisão em três canais não escreve nenhum `ResultadoNegociacao`.

Remaining risks:
  - **Front quebrado até T4.** `RevisaoCobranca.script` e `ValorJustoCardData.
    script` mudaram de forma; o app não compila até T4 atualizar `src/api/types.ts`
    e o `ScriptCard`. É o custo declarado da tarefa que muda comportamento em
    produção.
  - **Mudança de comportamento em produção.** A oferta saiu do primeiro contato
    nos canais escritos (chat/email): quem copiou o script na semana passada
    encontra outro texto. Deliberado (ADR 0021, item 5); T6 a declara no roadmap.
  - Copy de negociação nas três variantes ainda não passou por advogado — gate de
    pré-lançamento aberto, não declarado satisfeito.

Human decisions required:
  - Nenhuma nova além dos gates já registrados no plano.
```

## Desvios conscientes do spec

O único ponto fora dos `expected_areas` foi `backend/tests/test_caixa_integracao.py`: seus quatro
testes de oferta tratavam `script` como string e quebravam com a mudança de forma. Atualizá-los é
consequência direta e obrigatória da mudança de contrato desta tarefa (DoD: nenhum gate
desativado). Nenhuma regra de `domain/revisao.py`, de `valor_justo` ou de `_capacidade_para_oferta`
foi tocada — o valor da oferta continua o que era (o F-011 o mudará sozinho, PF-3).

## Oportunidades vistas e NÃO implementadas (fora de escopo)

- Atualizar `src/api/types.ts` e o `ScriptCard` para a forma nova — T4.
- Alinhar o regex de copy do front com o do backend — T6, PF-4.
- Emitir um card de segurança próprio no chat para quem não tem achado — caminho aberto, fora do
  escopo desta feature (PF-2).
