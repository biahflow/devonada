# BUILD REPORT — F-012 T3

```text
Status: BUILD_COMPLETE
Files changed:
  - backend/orm.py — `ResultadoNegociacao` (nova): append-only, no molde de
    `Renegociacao`, com `tenant_id`, `divida_id`, `canal`, `desfecho`,
    `valor_proposto`/`valor_obtido` opcionais, `renegociacao_id` opcional,
    `observacao` e `registrado_em`.
  - backend/schemas.py — `Canal`, `DesfechoNegociacao`, `RegistroNegociacaoInput`
    (com validador que recusa `renegociacaoId` fora do acordo),
    `ResultadoNegociacao`, `RespostaResultadoNegociacao`,
    `ListaResultadosNegociacao`.
  - backend/routers/negociacoes.py (novo) — `POST /v1/dividas/{id}/negociacoes`
    (201), `GET /v1/dividas/{id}/negociacoes` e `GET /v1/negociacoes`.
  - backend/main.py — importa e registra `negociacoes.router`.
  - backend/alembic/versions/a1c2e3f40b5d_resultado_negociacao.py (novo) —
    migração forward-only encadeada em `75331c212261`, cria a tabela com índices
    em `tenant_id` e `divida_id`.
  - backend/tests/test_negociacoes_api.py (novo) — 14 testes cobrindo T3-AC1 a
    T3-AC6, e a entrada da tabela na varredura de exclusão de conta.
  - docs/features/F-012-negociacao-por-canal/reports/T3.md (novo) — este relatório.

Validation executed:
  - cd backend && venv/bin/pytest → 676 passed, 23 warnings (baseline 662 + 14
    novos). Nenhuma falha; nenhum teste que passava parou de passar.
  - cd backend && venv/bin/pytest tests/test_negociacoes_api.py → 14 passed.
  - alembic heads → 75331c212261 (cabeça única, confirmada ANTES de escrever o
    `down_revision`, conforme o PLAN_DEVIATION de 20/08/2026).
  - Migração (T3-AC7), round-trip contra SQLite limpo, em isolamento a partir do
    pai carimbado (`alembic stamp 75331c212261`):
      upgrade head  → cria `resultado_negociacao` + índices `ix_..._tenant_id` e
        `ix_..._divida_id`;
      downgrade -1  → tabela desaparece;
      upgrade head  → recria; `alembic current` = a1c2e3f40b5d (head).
    Conferência do DDL contra `orm.ResultadoNegociacao.__table__`: mesmo conjunto
    de colunas e mesma nullability, campo a campo.

Validation skipped:
  - Round-trip da cadeia INTEIRA a partir do base contra SQLite: a suíte roda em
    SQLite (`Base.metadata.create_all`), mas o `alembic upgrade` desde o zero
    falha numa migração de dado ANTERIOR e alheia a esta tarefa (renda tipada,
    `482c266f5c6a`, usa `FALSE/TRUE` e SELECT sobre `perfil`, dialeto Postgres).
    Por isso o round-trip desta migração foi feito em isolamento, carimbando o
    pai — a migração nova em si é aditiva e SQLite-limpa. Contra Postgres não foi
    possível: não há Postgres nesta máquina (registrado como limitação de
    ambiente, não como sucesso presumido).

Unavailable capabilities:
  - Postgres local indisponível — round-trip contra Postgres não executado.

Assumptions:
  - `RegistroNegociacaoInput` recusa `renegociacaoId` quando `desfecho != acordo`
    (validador Pydantic, 422). O contrato pede "os demais o deixam nulo"; a
    leitura escolhida é a mais estrita — informar acordo num resultado que não
    teve acordo é dado contraditório, não silenciosamente descartável.
  - `valor_proposto` e `valor_obtido` opcionais e sem exigência cruzada: registrar
    recusa ou silêncio não exige valor (T3-AC2), que é o viés de benchmark que a
    entidade existe para desfazer.
  - Índice adicional em `divida_id` além do de `tenant_id` pedido: a leitura por
    dívida (`GET /v1/dividas/{id}/negociacoes`) filtra por ele. Não altera o
    contrato; é escolha de forma no molde das demais tabelas por dívida.
  - Rota de escrita nasce travada pela dependência global de assinatura
    (`main.py`), como toda POST — nenhuma linha a mais foi necessária.

Remaining risks:
  - `conftest.py` monta o schema com `create_all`, não pelo Alembic: uma migração
    divergente do ORM passaria a suíte verde. Mitigado pela conferência manual do
    DDL acima (T3-AC7), obrigatória por isso mesmo.
  - Sem Postgres local, uma divergência de dialeto só apareceria no round-trip de
    produção. A migração é aditiva e no estilo exato das existentes
    (`server_default=sa.text('now()')`), o que reduz a superfície.

Human decisions required:
  - Nenhuma nova. A modelagem (entidade nova × colunas aditivas) já foi decidida
    pela ADR 0021, item 6.
```

## Desvios conscientes do spec

Nenhum desvio de escopo, arquivo ou comportamento. `orm.Renegociacao` não foi tocada; o marco
`primeira_negociacao` continua nascendo do acordo em `parcelas.renegociar` e o registro de
resultado não o dispara (provado por `TestNaoDisparaMarco`). `routers/conta.py` não foi editado —
a tabela entra na varredura pelo `tenant_id`, provado por `TestEntraNaVarreduraDeExclusao`.

## Oportunidades vistas e NÃO implementadas (fora de escopo)

- Consumo do resultado na tela de renegociar e a leitura no front — T5.
- Qualquer agregação entre tenants ("credores que mais dão desconto") — fora de escopo por
  contrato.
- Script e canal na leitura — T1 e T2.
