# T4 — A tela de fonte de renda pergunta o que o tipo exige

## Identity

```text
feature_id: F-011
task_id: T4
parent_plan: docs/features/F-011-renda-tipada/plan.md
depends_on: T3
```

## Goal

**Um formulário que se adapta ao tipo escolhido** — não seis fluxos dedicados (decisão de
20/08/2026, PF-5 do plano). Seis comportamentos, um fluxo. O usuário já escolhe o tipo hoje; a
partir daqui a escolha muda o que o app pergunta.

## Scope

- `src/api/types.ts` — `impostoBps` e `diaPagamento` em `FonteRenda`; o tipo `EventoPrevisivel`.
- `src/api/caixa.ts` — as chamadas do CRUD de evento previsível, no estilo do arquivo.
- `src/hooks/useCaixa.ts` — as mutações novas, usando o invalidador que já existe.
- A tela de fonte de renda, com os campos mudando por tipo:
  - `clt` — líquido mensal, mais 13º e férias com mês e valor **declarados pelo usuário**;
  - `pj_hora` — taxa, horas e alíquota. Sem alíquota, a tela diz **"não está reservando imposto"**,
    lendo `impostoNaoDeclarado` do servidor — nunca exibindo `R$ 0,00`;
  - `autonomo` — renda típica em evidência, e o caminho para o compromisso percentual;
  - `beneficio` — valor fixo e dia de pagamento próprio;
  - `aluguel` — variável, e a vacância nomeada como um mês de recebimento zero;
  - `outro` — genérico, e a tela **diz** que é genérico.
- `src/test/screens/` — os quatro estados de tela e um caso por tipo.

## Out of Scope

- Compromisso percentual no Caixa e a exibição do mês âncora: **T5**.
- **Seis fluxos dedicados** com tela e copy próprias. Ficaram para pós-MVP por decisão de
  20/08/2026, a serem decididos com dado de uso real em vez de no papel.
- **Calcular qualquer valor no cliente.** Taxa × horas, imposto e renda típica chegam prontos do
  servidor (guardrail 1.2). O cliente não multiplica nem aplica percentual.
- **Inventar verbete de design system.** O verbete é escrito em T6, a partir do que esta tarefa
  entregou. Documentar antes de existir é como o `ScriptCard` ficou dois milestones em "Ainda só
  especificação".
- Sugerir valor, faixa ou percentual em qualquer copy.
- Backend.

## Acceptance Criteria

- **T4-AC1** — escolher cada um dos seis tipos muda os campos apresentados, e há teste por tipo.
- **T4-AC2** — fonte `pj_hora` sem alíquota exibe **"não está reservando imposto"**, lido de
  `impostoNaoDeclarado`. Um teste falha se `R$ 0,00` aparecer no lugar. É a ADR 0009 aplicada à tela.
- **T4-AC3** — os quatro estados de tela — carregando, erro, vazio e conteúdo — têm teste.
- **T4-AC4** — controles novos têm alvo de toque de 48pt e `accessibilityLabel` quando não há texto
  visível.
- **T4-AC5** — nenhum valor monetário é calculado no cliente.
- **T4-AC6** — os seis gates do front passam.

## Validation

```text
baseline: npx jest → 45 suítes / 539 testes (20/08/2026), MAIS o que T1–T3 acrescentaram ao
          backend (o Jest não muda com elas). Meça de novo antes de começar.
          O Jest imprime o resumo em ~5s e NÃO ENCERRA o processo, por handles abertos
          preexistentes. Não conclua que a suíte travou; use --forceExit se precisar.

required: npm run typecheck ; npm run lint ; npm test ; npm run bundle:check ;
          npm run palette:check ; npm run digits:check
```

## Required Capabilities

```text
READ:     repositório inteiro
WRITE:    src/api/types.ts · src/api/caixa.ts · src/hooks/useCaixa.ts ·
          src/components/caixa/ · app/(tabs)/caixa/ · src/test/screens/
VALIDATE: npm scripts
COMMIT:   allowed — um commit
```

## Context to Read First

- `docs/design-system.md` — os componentes que já existem (`FormField`, `CurrencyInput`, `Feedback`,
  `LoadingState`, `EmptyState`, `ErrorState`). **Reuse; não crie primitiva nova.** Note que não há
  verbete de renda tipada — é esperado, e T6 o escreve.
- `src/components/caixa/RespiroCard.tsx` e `app/(tabs)/caixa/respiro.tsx` — o molde mais recente de
  card com dois estados e tela de declaração.
- `docs/agent-guidelines.md` — a cadeia `api/` → `hooks/` → `screens/` → `components/`. Componente
  não chama `src/api/` direto.
- `docs/guardrails.md`, seção 1.2 e 1.3 — o app não calcula derivado, e número sem procedência vira
  "ainda não calculado".

## Known Risks

- **Exibir `R$ 0,00` como se fosse reserva de imposto.** É o modo de falha que `impostoNaoDeclarado`
  existe para impedir, e T4-AC2 é o teste.
- **Inchar.** Seis tipos × quatro estados é a superfície que a ADR 0021 nomeia como a que mais pode
  crescer. Um formulário, campos condicionais.
- **`bundle:check` pega import que nenhum teste importa.** Rode-o de verdade; ele é o gate que vê
  módulo que não resolve.
- Divide `src/api/types.ts` com T5. Se as duas rodarem juntas, coordene.

## Human Gates

- Validação em device dos fluxos novos — leitura, teclado sobre campo de valor, acessibilidade.
  **Nenhum agente declara este gate satisfeito**, e nenhum gate automático o substitui.

## Reporting

Termine com o `BUILD REPORT` completo, e diga explicitamente o que **não** foi validado em device.
**Grave-o também em `docs/features/F-011-renda-tipada/reports/T4.md`**, versionado neste commit.
