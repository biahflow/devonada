# T6 — Provar o efeito nos quatro consumidores, provar a regressão, e deixar a documentação dizendo a verdade

## Identity

```text
feature_id: F-011
task_id: T6
parent_plan: docs/features/F-011-renda-tipada/plan.md
depends_on: T2, T4, T5
```

## Goal

Compromisso percentual declarado derruba `capacidade_maxima` e, com ela, **quatro** consumidores —
sem que nenhum dos quatro arquivos seja tocado. Ação a distância não aparece em diff, e é o defeito
que passou por quatro gates verdes no M7.2. Esta tarefa escreve o teste que o torna visível, o teste
que prova que quem não declarou nada não mudou, e a documentação que declara a mudança.

## Scope

- `backend/tests/test_caixa_integracao.py` — `TestCompromissoNosQuatroConsumidores`, gêmeo de
  `TestRespiroNosTresConsumidores` (280-409) e com **um teste a mais**:
  1. teto do simulador (`routers/simulacoes._validar_aporte`, via `capacidade_atual` em `:47`);
  2. `margemDisponivel` do painel (`routers/resumo.py:94`, exposta em `:277`);
  3. card `plano_sugerido` do chat (`routers/chat.py:165-166`);
  4. **a oferta do script de negociação** (`routers/revisao.py:176`,
     `_capacidade_para_oferta`, que lê `caixa.capacidade_hoje`) — PF-1 do plano.
- `backend/tests/test_caixa_integracao.py` — o gêmeo de
  `test_quem_nao_declarou_respiro_tem_os_tres_numeros_de_antes` (394-409), agora com quatro.
- `src/test/screens/` — teste de copy da renda tipada, no molde de `respiro-copy.test.tsx` (36
  casos, prova por injeção): quebra em copy que sugira valor, faixa ou percentual de compromisso, e
  em tom de prestação de contas sobre mês fraco.
- `docs/api-contract.md` — seção **3.14** com os endpoints do F-011 e **Bloco 14** na fila da seção 4.
- `docs/design-system.md` — verbete de **renda tipada** e do card de compromisso percentual,
  escritos a partir do que T4 e T5 entregaram. Hoje não existe nenhum.
- `docs/domain.md` — a tabela do verbete `tipo de renda` (142-154) passa a cobrir os **seis** valores;
  hoje ela lista três (PF-8 do plano).
- `roadmap.md` — os dois itens de F-011 no M12 marcados conforme o entregue, e a **mudança de
  comportamento a declarar**, no molde do que o M11 fez.
- `docs/inventario.md` e `docs/engineering-os-adoption.md` — baseline de validação novo, **MEDIDO**.
- `docs/features/F-011-renda-tipada/evidence.md` (novo) — os seis `BUILD REPORT` com atribuição por
  tarefa, baseline, validação integrada, commits e `PLAN_DEVIATION`.

## Out of Scope

- **Código de feature.** T6 escreve teste e documento. Se um teste novo falhar por defeito de
  implementação, **pare e reporte** — não conserte área de outra tarefa.
- **Marcar como validado em device o que não foi visto em aparelho.** Nenhum gate deste repositório
  prova que a tela está legível ou que cabe na tela.
- **Fundir os `BUILD REPORT` num resumo.** A atribuição por tarefa é o que o Reviewer precisa.
- Copiar baseline de outro documento. Ela sai de execução, sempre.

## Acceptance Criteria

- **T6-AC1** — o teste cruzado prova que os **quatro** consumidores caem, e que o teto do simulador
  desce **exatamente** o compromisso declarado.
- **T6-AC2** — o teste de regressão prova que quem não declarou percentual nem alíquota por fonte
  tem os quatro números idênticos aos de antes do F-011.
- **T6-AC3** — o teste de copy quebra por injeção nas superfícies novas.
- **T6-AC4** — nenhum documento canônico afirma validação em device que não aconteceu.
- **T6-AC5** — `evidence.md` preserva os seis Builder Reports com atribuição por tarefa.
- **T6-AC6** — os sete perfis passam, e o relato distingue o executado do pulado.

## Validation

```text
baseline: MEÇA os dois, não copie:
          npx jest  (ou npm test)
          cd backend && venv/bin/pytest

required: npm run typecheck ; npm run lint ; npm test ; npm run bundle:check ;
          npm run palette:check ; npm run digits:check ; cd backend && venv/bin/pytest
```

## Required Capabilities

```text
READ:     repositório inteiro
WRITE:    backend/tests/test_caixa_integracao.py · src/test/screens/ · docs/api-contract.md ·
          docs/design-system.md · docs/domain.md · roadmap.md · docs/inventario.md ·
          docs/engineering-os-adoption.md · docs/features/F-011-renda-tipada/evidence.md
VALIDATE: todos os sete perfis
COMMIT:   allowed — um commit
```

## Context to Read First

- `backend/tests/test_caixa_integracao.py:280-409` — a classe inteira que é o molde, com os helpers
  `_declarar`, `_aporte_maximo`, `_margem_do_painel` e `_card_de_plano` (301-324). Você acrescenta
  um quinto helper para a oferta do script.
- `backend/tests/test_caixa.py:433-495` — `TestRegressaoSemRespiro`, o molde da regressão unitária.
- `src/test/screens/respiro-copy.test.tsx` — 36 casos, prova por injeção. É o molde mais recente.
- `docs/features/F-010-respiro/evidence.md` — o molde do documento de evidência.
- `roadmap.md`, seção M11, "Mudança de comportamento a declarar" — o molde do parágrafo a escrever.

## Known Risks

- **Escrever o teste cruzado só para três consumidores** porque a ADR e o contrato dizem três. São
  quatro; o PF-1 do plano registra a descoberta e a decisão.
- **Baseline copiada.** Ela envelhece em silêncio e faz o Builder atribuir à própria mudança um
  teste que já existia. Foi o PF-9 do F-010.
- **Documentar o que não existe.** O verbete de design system sai do que T4 e T5 entregaram, não do
  que o plano descreveu.

## Human Gates

- **Validação em device de tudo que T4 e T5 entregaram.** É o gate que fecha a feature; nenhum agente
  o declara satisfeito e nenhum documento deste commit pode declará-lo.
- A passagem de `READY_FOR_HUMAN_REVIEW` para `DONE`. Nenhum executor a faz.

## Reporting

Termine com o `BUILD REPORT` completo. **Grave-o também em
`docs/features/F-011-renda-tipada/reports/T6.md`**, versionado neste commit — e a `evidence.md`
referencia os seis arquivos de `reports/`, em vez de transcrevê-los de memória.
