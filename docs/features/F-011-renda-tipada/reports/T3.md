# BUILD REPORT — F-011 · T3

```text
feature_id: F-011
task_id: T3
contrato: docs/features/F-011-renda-tipada/tasks/T3.md
harness: Claude Code (Builder, execução em 27/08/2026)
```

BUILD REPORT

Status: BUILD_COMPLETE

Files changed:

- `backend/schemas.py` — `compromissoPercentualBps` em `MetasCaixa` (0–10000);
  `compromissoPercentualBps`, `compromissoPercentual`, `impostoNaoDeclarado` e
  `mesAncoraRenda` em `Caixa`; `impostoBps` e `diaPagamento` em `NovaFonteRenda`,
  `FonteRendaPatch` e (por herança) `FonteRenda`; `TipoEventoPrevisivel` e os
  schemas de evento previsível (novo/patch/response/lista).
- `backend/routers/caixa.py` — os quatro campos novos em `_caixa_schema`;
  `impostoBps`/`diaPagamento` em `_fonte_schema`, `criar_fonte` e no mapa de
  `editar_fonte`; `compromissoPercentualBps` em `_metas_schema` e `gravar_metas`,
  com o `422` do piso legal copiado do registro de `gravar_respiro` (pt-BR, sem
  valor no corpo, com `campo` e a guarda `preenchimento != "vazio"`); CRUD de
  evento previsível (`GET/POST/PATCH/DELETE`) no estilo do CRUD de fontes, com
  `_buscar_evento` devolvendo 404 e snapshot após cada mutação.
- `backend/tests/test_caixa_api.py` — `TestCamposNovosDoCaixa`,
  `TestCompromissoPercentualNasMetas`, `TestAliquotaPorFonteNaAPI`,
  `TestEventoPrevisivelAPI` e `TestCompromissoNoSimulador` (+15 testes).
- `docs/api-contract.md` — o contrato do endpoint documentado NESTE commit
  (CLAUDE.md): campos novos em `GET /v1/caixa`, `/fontes` e `PUT /metas`, mais a
  rota `/v1/caixa/eventos-previsiveis`. A seção 3.14 e o Bloco 14 formais são de
  T6, como manda o contrato.
- `docs/features/F-011-renda-tipada/reports/T3.md` — este relatório.

Acceptance criteria:

- T3-AC1 — `test_get_caixa_traz_os_quatro_campos_do_M12`. VERDE.
- T3-AC2 — `test_grava_e_devolve_o_percentual...` +
  `test_percentual_que_invade_o_piso_legal_devolve_422_sem_valor` (mensagem
  pt-BR, sem `R$`, sem dígito, com `campo`). VERDE.
- T3-AC3 — `test_bps_negativo_e_rejeitado` e `test_bps_acima_de_10000...`. VERDE.
- T3-AC4 — `test_post_e_patch_aceitam_imposto_bps_e_dia_pagamento` +
  `test_ausentes_a_fonte_continua_como_hoje_com_o_perfil_de_fallback`. VERDE.
- T3-AC5 — `TestEventoPrevisivelAPI.test_crud_completo` +
  `test_nao_muda_nenhum_numero_da_cascata` (antes == depois). VERDE.
- T3-AC6 — `test_evento_de_outro_tenant_devolve_404_e_nao_403` +
  `test_fonte_de_outro_tenant_no_post_devolve_404`. VERDE.
- T3-AC7 — `TestCompromissoNoSimulador`: aporte aceito passa a ser recusado após
  o compromisso. `git diff --stat backend/routers/simulacoes.py` VAZIO (abaixo).
- T3-AC8 — suíte inteira verde.

Validation executed:

```text
BASELINE (após T2): 678 passed, 23 warnings
FINAL (após T3):    693 passed, 25 warnings

git diff --stat main -- backend/routers/simulacoes.py → vazio (T3-AC7)
```

- As 25 warnings = 23 preexistentes + 2 novas da MESMA classe já onipresente no
  arquivo (`StarletteDeprecationWarning: 'HTTP_422_UNPROCESSABLE_ENTITY'`),
  emitidas pelo `422` novo de `gravar_metas`. O contrato de T3 manda copiar o
  registro EXATO de `caixa.py:982-997`, que usa essa constante; usar a substituta
  romperia a consistência do arquivo. Nenhum aviso de outra natureza.

Validation skipped:

- Gates de front (T3 não toca `src/`) e Postgres (worktree roda SQLite; ver
  Environment no relatório de T2). Os testes de T3 usam a fixture `sessao`
  (SQLite em memória) e o `TestClient`.

Unavailable capabilities: Postgres local.

Assumptions:

- `TipoEventoPrevisivel = decimo_terceiro | ferias | outro`. O contrato dá "tipo"
  sem enumerar; escolhi os dois nomeados da ADR 0021 mais um genérico.
- Mutação de evento previsível grava snapshot "como toda rota do módulo já faz"
  (contrato) — a foto é idêntica à anterior, porque o evento não entra na
  cascata; T3-AC5 prova que o número lido não muda.
- Faixa de `compromissoPercentualBps` como Field (0–10000), no molde de
  `impostoBps`/`rendimentoEsperadoBps`; o `422` do piso, que depende da renda,
  fica no router.

Remaining risks:

- `caixa_snapshot` não ganhou coluna para `imposto_nao_declarado`/`mes_ancora`
  (não pedido); o compromisso percentual já tinha coluna (T1).
- `src/` ainda não consome os campos novos — T4/T5.

Human decisions required:

- Mudança de contrato de API (aditiva), documentada neste commit. A seção 3.14 e
  o Bloco 14 formais são de T6.
```
