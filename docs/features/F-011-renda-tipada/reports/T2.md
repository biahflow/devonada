# BUILD REPORT — F-011 · T2

```text
feature_id: F-011
task_id: T2
contrato: docs/features/F-011-renda-tipada/tasks/T2.md
harness: Claude Code (Builder, execução em 27/08/2026)
```

BUILD REPORT

Status: BUILD_COMPLETE

Files changed:

- `backend/domain/renda.py` (novo) — `ComportamentoRenda` e uma função pura por
  tipo (`clt`, `pj_hora`, `autonomo`, `beneficio`, `aluguel`, `outro`), mais o
  dispatcher `comportamento(tipo)` com fallback genérico. Cada função declara
  FONTE (13º/férias em `clt`) ou "dado do usuário" no docstring; nenhum
  coeficiente financeiro nasce aqui. Os seis comportamentos são distintos por
  assinatura, não só pelo rótulo `tipo`.
- `backend/domain/caixa.py` — `renda_tipica` passa a devolver
  `(valor, origem, mes_ancora)` a partir de recebimentos `(mês, valor)`; a regra
  do `min()` NÃO muda. `EntradaCaixa` e `Caixa` ganham `mes_ancora_renda` e
  `imposto_nao_declarado` (aditivos); `calcular_caixa` os propaga da entrada sem
  cálculo, como já faz com `origem_renda`.
- `backend/leitura.py` — a query de recebimentos seleciona `(mes, valor)` em vez
  de só `valor`, para o mês âncora existir (PF-3). `montar_entrada_caixa` propaga
  o mês âncora da última fonte histórica e acende `imposto_nao_declarado` quando
  uma fonte que reserva imposto (`domain.renda.comportamento(...).reserva_imposto`,
  hoje só `pj_hora`) está ativa sem alíquota própria e sem fallback do `Perfil`.
- `backend/tests/test_domain.py` — `TestComportamentoDeRenda`: um teste por tipo,
  o teste de distinção dos seis comportamentos, o dispatcher com fallback e o
  teste que exige FONTE ou "dado do usuário" no docstring de cada função.
- `backend/tests/test_caixa.py` — `TestRendaTipica` migrada para a aridade nova
  (valor e origem inalterados); `TestCasoRealPJ` migrada para recebimentos
  `(mês, valor)`; `TestImpostoNaoDeclaradoNaLeitura` e `TestMesAncoraNaLeitura`
  (com banco), cobrindo o par imposto-zero/sinal-ligado, o mês âncora e o mês
  zerado que zera só a fonte dele.
- `docs/features/F-011-renda-tipada/reports/T2.md` — este relatório.

Acceptance criteria:

- T2-AC1 — `TestComportamentoDeRenda`, um teste por tipo + `len({...}) == 6` de
  comportamentos distintos. VERDE.
- T2-AC2 — `test_pj_hora_sem_aliquota_reserva_zero_e_acende_o_sinal`:
  `imposto_reservado == 0` E `imposto_nao_declarado is True`. VERDE.
- T2-AC3 — `test_com_historico_usa_o_PIOR_mes...` devolve o mês âncora; sem
  histórico devolve `None`. VERDE.
- T2-AC4 — `TestRendaTipica` prova a mesma regra depois da mudança de aridade:
  valor e origem inalterados. VERDE.
- T2-AC5 — `test_toda_funcao_de_tipo_declara_fonte_ou_dado_do_usuario`. VERDE.
- T2-AC6 — `test_um_mes_zerado_zera_so_a_fonte_dele_e_nao_as_outras`. VERDE.
- T2-AC7 — suíte inteira verde (abaixo).

Validation executed:

```text
BASELINE (27/08/2026, em main, antes de T2)
  cd backend && venv/bin/pytest → 662 passed, 23 warnings

FINAL (depois de T2)
  cd backend && venv/bin/pytest → 678 passed, 23 warnings
  (+16 testes de T2. Mesmos 23 avisos preexistentes: HTTP_422 depreciado,
  Starlette/httpx, InsecureKeyLength. Nenhum novo.)
```

Validation skipped:

- Gates de front (`typecheck`, `lint`, `test`, `bundle:check`, `palette:check`,
  `digits:check`). T2 não toca `src/`.
- Postgres. O venv da worktree roda a suíte em SQLite (o default do conftest); o
  `postgres`/`pysqlite3` não está disponível aqui — ver "Environment/Ambiente".
  Os testes novos de T2 tocam banco via a fixture `sessao` (SQLite em memória) e
  não dependem de dialeto Postgres.

Environment/Ambiente:

- `python3` do sistema é 3.9 e `requirements.txt` exige ≥3.10; `pysqlite3==0.6.0`
  não compila (falta header de SQLite novo). Venv criado com `uv venv --python
  3.12` e `requirements.txt` MENOS `pysqlite3` (não importado por teste nenhum —
  o dialeto `sqlite+pysqlite` usa o `sqlite3` da stdlib). `requirements.txt` NÃO
  foi modificado. Baseline reproduz o 662 do repo principal.

Unavailable capabilities: Postgres local (rodado só em SQLite).

Assumptions:

- `mes_ancora_renda` da entrada é o da ÚLTIMA fonte histórica do laço. Para quem
  tem uma fonte só — o caso da copy "dimensionado pelo pior mês, que foi março" —
  é o único, sem ambiguidade. Declarado no comentário de `leitura.py`.
- `reserva_imposto` é `True` apenas para `pj_hora`: é o único tipo em que o
  modelo espera bruto (taxa × horas) com alíquota à parte; os demais o usuário
  declara líquido. É o que faz `imposto_nao_declarado` casar exatamente com a
  T2-AC2.
- `ComportamentoRenda` não wira a coluna `fonte_renda.variavel` (PF-6): reconciliar
  as duas fontes de verdade de "essa renda oscila?" está fora de escopo.

Remaining risks:

- `docs/domain.md` (verbete `tipo de renda`, três valores) e `docs/design-system.md`
  ainda não refletem os seis tipos — é T6, por contrato.
- `mes_ancora_renda`/`imposto_nao_declarado` ainda não têm superfície HTTP nem de
  tela; T3 expõe pela API e T4/T5 exibem.

Human decisions required: none além dos gates do plano. T2 não muda contrato de API.
```
