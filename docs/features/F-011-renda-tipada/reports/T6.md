# BUILD REPORT — F-011 · T6

```text
feature_id: F-011
task_id: T6
contrato: docs/features/F-011-renda-tipada/tasks/T6.md
harness: Claude Code (Builder, execução em 27/08/2026)
```

BUILD REPORT

Status: BUILD_COMPLETE

Files changed:

- `backend/tests/test_caixa_integracao.py` — `TestCompromissoNosQuatroConsumidores`,
  gêmeo de `TestRespiroNosTresConsumidores` com UM consumidor a mais: o teto do
  simulador, a `margemDisponivel` do painel, o card `plano_sugerido` do chat e a
  **oferta do script de negociação** (`routers/revisao`, PF-1). Mais a regressão
  gêmea, agora com quatro números. (+5 testes.)
- `src/test/screens/renda-tipada-copy.test.tsx` (novo) — a varredura de copy da
  renda tipada, prova por injeção (25 casos): quebra em sugestão de valor/faixa/
  percentual de compromisso e em tom de prestação de contas sobre mês fraco.
- `docs/api-contract.md` — seção **3.14** (F-011) e **Bloco 14** na fila da
  seção 4.
- `docs/design-system.md` — verbetes de renda tipada, `CompromissoCard` e mês
  âncora, escritos a partir do que T4 e T5 entregaram.
- `docs/domain.md` — a tabela `tipo de renda` passa a cobrir os **seis** valores;
  `renda típica` ganha o mês âncora.
- `roadmap.md` — os dois itens de F-011 marcados `[x]`, mais a baseline medida e a
  "Mudança de comportamento a declarar" (quatro consumidores).
- `docs/inventario.md` e `docs/engineering-os-adoption.md` — baseline de
  validação novo, MEDIDO em 27/08/2026.
- `docs/features/F-011-renda-tipada/evidence.md` (novo) — os seis BUILD REPORT
  referenciados por tarefa, validação integrada, commits e `PLAN_DEVIATION`.
- `docs/features/F-011-renda-tipada/reports/T6.md` — este relatório.

Acceptance criteria:

- T6-AC1 — `TestCompromissoNosQuatroConsumidores` prova os QUATRO caindo, e o
  teto do simulador descendo EXATAMENTE o compromisso. VERDE.
- T6-AC2 — `test_quem_nao_declarou_compromisso_tem_os_quatro_numeros_de_antes`.
  VERDE.
- T6-AC3 — o sweep de copy quebra por injeção nas superfícies novas. VERDE.
- T6-AC4 — nenhum documento afirma validação em device; todos marcam o gate como
  aberto.
- T6-AC5 — `evidence.md` referencia os seis reports com atribuição por tarefa.
- T6-AC6 — os sete perfis passam (abaixo), e o relato distingue executado de
  pulado.

Validation executed (os sete perfis, medidos no fechamento):

```text
npm run typecheck   → OK
npm run lint        → OK
npm test (jest)     → 48 suítes / 589 testes, verdes (--forceExit)
npm run bundle:check   → exit 0
npm run palette:check  → exit 0 (nenhum token de cor tocado no F-011)
npm run digits:check   → exit 0 (nenhuma fonte tocada)
backend venv/bin/pytest → 698 passed, 27 warnings (SQLite)

BASELINE de entrada (main, 27/08/2026): 46 suítes / 541 Jest e 662 pytest.
```

Não executado / pulado, dito explicitamente:

- **Postgres.** Não está disponível nesta worktree; a suíte rodou em SQLite (o
  padrão do conftest). Ver a nota de ambiente no relatório de T2 e em
  `engineering-os-adoption.md`. Continua gate de release.
- **Device.** Nenhuma tela do F-011 foi vista em aparelho. É o gate humano que
  fecha o M12; nenhum documento deste commit o declara satisfeito.
- **Alembic downgrade/upgrade** já fora verificado por T1 (Postgres local, na
  execução original de T1); não repetido aqui por ausência de Postgres.

Unavailable capabilities: Postgres local, device físico.

Assumptions:

- O sweep de copy usa 25 casos (6 tipos de fonte + 4 estados do card + 2 da
  declaração + 13 de mês âncora/origem), não os 36 do respiro — a superfície da
  renda tipada é outra. A prova por injeção e a lista única de termos proibidos
  seguem o molde.
- O "não sugere percentual" varre só o TEXTO VISÍVEL, ignorando `width: '100%'`
  de layout, que um `JSON.stringify` cru confundiria com percentual sugerido.

Remaining risks:

- `docs/backend.md` já ganhou as linhas do compromisso e do piso legal em commit
  anterior de T1 (`55ebdc6`); não precisou de mudança aqui.
- A passagem `READY_FOR_HUMAN_REVIEW` → `DONE` é gate humano e NÃO foi feita.

Human decisions required:

- Validar em device os fluxos de T4 e T5. Fecha a feature.
- A promoção para `DONE` depois da revisão humana.
```
