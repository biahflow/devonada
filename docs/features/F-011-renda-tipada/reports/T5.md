# BUILD REPORT — F-011 · T5

```text
feature_id: F-011
task_id: T5
contrato: docs/features/F-011-renda-tipada/tasks/T5.md
harness: Claude Code (Builder, execução em 27/08/2026)
```

BUILD REPORT

Status: BUILD_COMPLETE

Files changed:

- `src/api/types.ts` — `compromissoPercentualBps`, `compromissoPercentual` e
  `mesAncoraRenda` na interface `Caixa`; `compromissoPercentualBps` em `MetasCaixa`.
  (`impostoNaoDeclarado` já entrara em T4.)
- `src/components/caixa/CompromissoCard.tsx` (novo) — card com DOIS ESTADOS, no
  molde do `RespiroCard`: sem percentual, convida sem sugerir valor/faixa; com
  percentual, mostra o bps e o valor em centavos que o servidor mandou.
- `app/(tabs)/caixa/compromisso.tsx` (novo) — a declaração, no molde de
  `respiro.tsx`. Grava por `PUT /v1/caixa/metas` MANDANDO AS OUTRAS METAS DE
  VOLTA (a rota sobrescreve tudo); o `422` do piso vem pronto do servidor.
- `app/(tabs)/caixa/index.tsx` — renderiza o `CompromissoCard` e faz a linha de
  origem da renda dizer qual mês a ancorou (`mesAncoraRenda`), sumindo quando a
  origem é `informada`.
- `src/test/mocks.ts` — `compromissoPercentualBps`, `compromissoPercentual` e
  `mesAncoraRenda` (nulos) em `umCaixa`.
- `src/test/screens/caixa.test.tsx` — `CompromissoCard` (dois estados), o mês
  âncora (presença e ausência) e a tela de declaração (4 estados, a11y, o PUT que
  preserva as outras metas, o 422 do piso).
- `docs/features/F-011-renda-tipada/reports/T5.md` — este relatório.

Acceptance criteria:

- T5-AC1 — sem percentual, o card convida e não mostra número (o legend do
  estado declarado está ausente). VERDE.
- T5-AC2 — com percentual, mostra `10,00%` e `R$ 940,00`, ambos do servidor.
  VERDE.
- T5-AC3 — origem + mês âncora aparecem no `pior_mes_registrado` (`que foi
  mar/26`) e somem no `informada`. VERDE.
- T5-AC4 — os quatro estados de tela têm teste (carregando/erro da declaração;
  vazio/conteúdo cobertos pela tela de caixa). VERDE.
- T5-AC5 — `accessibilityLabel` no `PercentInput` novo. VERDE.
- T5-AC6 — os seis gates de front passam (abaixo).

Validation executed:

```text
BASELINE (main, 27/08/2026): 46 suítes / 541 testes (com a flutuação de
  open-handles/act já descrita em T4).

FINAL (após T5)
  npm run typecheck  → OK
  npm run lint       → OK
  npm test (jest)    → 47 suítes / 564 testes, todos verdes
  npm run bundle:check  → exit 0
  npm run palette:check → exit 0 (nenhum token de cor tocado)
  npm run digits:check  → exit 0 (nenhuma fonte tocada)
```

Nota: o Jest imprime o resumo em ~5s e NÃO encerra o processo (open handles
preexistentes); usei `--forceExit` para ler o resultado, sem alterar contagem.

Validation skipped: backend (T5 não toca `backend/`).

Not validated in device (declarado explicitamente):

- Leitura do card, teclado sobre o campo de percentual, safe area e
  acessibilidade real. Gate humano do contrato; nenhum agente o declara
  satisfeito.

Unavailable capabilities: device físico.

Assumptions:

- A declaração grava por `PUT /v1/caixa/metas` reenviando as demais metas — a
  rota sobrescreve todos os campos, e mandar só o percentual apagaria imposto,
  reserva e aposentadoria. Testado.
- `mesAncoraRenda` é exibido com `formatMesCurto` (`mar/26`), consistente com o
  resto do app; a copy do contrato ("que foi março") é ilustrativa.

Remaining risks:

- O verbete de design system do card de compromisso é escrito em T6, a partir do
  que esta tarefa entregou.
```
