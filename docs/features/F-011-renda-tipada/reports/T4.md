# BUILD REPORT — F-011 · T4

```text
feature_id: F-011
task_id: T4
contrato: docs/features/F-011-renda-tipada/tasks/T4.md
harness: Claude Code (Builder, execução em 27/08/2026)
```

BUILD REPORT

Status: BUILD_COMPLETE

Files changed:

- `src/api/types.ts` — `impostoBps` e `diaPagamento` em `FonteRenda`; o tipo
  `EventoPrevisivel` e `TipoEventoPrevisivel`; `impostoNaoDeclarado` na interface
  `Caixa` (ver PLAN_DEVIATION).
- `src/api/caixa.ts` — CRUD de evento previsível (`listar/criar/atualizar/excluir`)
  e `NovoEventoPrevisivel`.
- `src/hooks/useCaixa.ts` — `useEventosPrevisiveis`, `useCriarEventoPrevisivel`,
  `useAtualizarEventoPrevisivel`, `useExcluirEventoPrevisivel` e a chave
  `eventosPrevisiveis`, usando o invalidador existente.
- `app/(tabs)/caixa/renda.tsx` — UM FORMULÁRIO QUE SE ADAPTA AO TIPO (não seis
  fluxos): `PERFIL_POR_TIPO` decide o rótulo do valor, a alíquota (`pj_hora`), o
  dia de pagamento (`beneficio`), os eventos previsíveis 13º/férias (`clt`), o
  caminho do compromisso percentual (`autonomo`), a copy de vacância (`aluguel`)
  e a nota de genérico (`outro`). A fonte `pj_hora` sem alíquota exibe "não está
  reservando imposto", lido de `impostoNaoDeclarado` — nunca `R$ 0,00`.
- `src/test/mocks.ts` — `impostoNaoDeclarado: false` em `umCaixa`.
- `src/test/screens/renda.test.tsx` (novo) — quatro estados, um caso por tipo, o
  par pj_hora-sem-alíquota, ausência de sugestão de faixa e os `accessibilityLabel`.
- `docs/features/F-011-renda-tipada/reports/T4.md` — este relatório.

Acceptance criteria:

- T4-AC1 — um teste por tipo em "formulário que se adapta ao tipo". VERDE.
- T4-AC2 — `pj_hora sem alíquota` mostra "não está reservando imposto" e nunca
  `R$ 0,00`. VERDE.
- T4-AC3 — os quatro estados (carregando/erro/vazio/conteúdo) têm teste. VERDE.
- T4-AC4 — controles novos com `accessibilityLabel` (via `PercentInput`,
  `CurrencyInput`, `FormField`, `OptionGroup`, que já garantem ≥48pt). VERDE.
- T4-AC5 — nenhum valor calculado no cliente: alíquota, imposto e renda típica
  vêm do servidor; a busca por faixa/percentual sugerido não acha nada. VERDE.
- T4-AC6 — os seis gates de front passam (abaixo).

Validation executed:

```text
BASELINE (main, medido em 27/08/2026)
  npx jest → 46 suítes / 541 testes (com flutuação conhecida de open-handles/act:
            a 1ª execução acusou 4 falhas que não reproduziram na 2ª — o Jest não
            encerra sozinho depois do resumo, é preexistente).
  typecheck, lint, bundle:check, palette:check, digits:check → verdes.

FINAL (após T4)
  npm run typecheck  → OK
  npm run lint       → OK
  npm test (jest)    → 47 suítes / 555 testes, todos verdes (+1 suíte renda, +14)
  npm run bundle:check  → exit 0
  npm run palette:check → exit 0 (nenhum token de cor tocado)
  npm run digits:check  → exit 0 (nenhuma fonte tocada)
```

Validation skipped: backend (T4 não toca `backend/`).

Not validated in device (declarado explicitamente):

- Leitura, teclado sobre campo de valor, safe area e acessibilidade real dos
  fluxos por tipo. É gate humano do contrato; NENHUM agente o declara satisfeito,
  e nenhum gate automático o substitui.

Unavailable capabilities: device físico.

Assumptions:

- Sem campos separados de "taxa" e "horas" no modelo do backend: a fonte guarda
  `valorTipicoInformado`, e a UX do `pj_hora` pede esse valor mais a alíquota. O
  "taxa × horas" do contrato é conceitual; inventar colunas seria fora de escopo.
- `impostoNaoDeclarado` é global; para uma fonte `pj_hora` com `impostoBps` nulo,
  o aviso só aparece quando o sinal está ligado (o que implica não haver fallback
  de perfil) — então a fonte sem a sua realmente não reserva.
- Os eventos previsíveis são geridos dentro do ramo `clt` do formulário (13º e
  férias), com `fonteId` nulo — o modelo permite evento sem fonte.

PLAN_DEVIATION:

- `impostoNaoDeclarado` foi adicionado à interface `Caixa` em T4, e não em T5
  como o plano nominalmente listou. Motivo: T4-AC2 exige que a tela de renda leia
  esse campo do servidor, e T4 depende de T3, não de T5. Os outros três campos da
  `Caixa` (`compromissoPercentualBps`, `compromissoPercentual`, `mesAncoraRenda`)
  seguem para T5. Sem impacto de escopo — execução sequencial, sem conflito de
  merge.

Remaining risks:

- O verbete de design system de renda tipada é escrito em T6, a partir do que
  esta tarefa entregou (contrato). A tela existe; o doc ainda não.
```
