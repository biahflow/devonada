# BUILD REPORT — F-011 · T1

```text
feature_id: F-011
task_id: T1
contrato: docs/features/F-011-renda-tipada/tasks/T1.md
harness: Claude Code (execução em 20/08/2026)
```

BUILD REPORT

Status: BUILD_COMPLETE

Files changed:

- `backend/domain/caixa.py` — `compromisso_percentual_bps` e `imposto_por_fonte` em
  `EntradaCaixa` (aditivos, default `None`); `compromisso_percentual_bps` e
  `compromisso_percentual` em `Caixa`; a cascata passa a subtrair o compromisso na mesma
  posição dos potes e do respiro, e o imposto passa a aceitar o somatório por fonte;
  `percentual_invade_o_piso` nasce no molde de `respiro_invade_o_piso`, com a FONTE do piso.
- `backend/orm.py` — `perfil.compromisso_percentual_bps`; `fonte_renda.imposto_bps` e
  `fonte_renda.dia_pagamento`; tabela `evento_previsivel` (com `tenant_id`).
- `backend/alembic/versions/482c266f5c6a_renda_tipada_e_evento_previsivel.py` — a migração da
  onda, encadeada em `116f2181bdda`; todas as colunas nullable, nenhum dado migra.
- `backend/leitura.py` — `montar_entrada_caixa` apura o imposto fonte a fonte no laço que já
  percorre as fontes e lê `perfil.compromisso_percentual_bps`; a leitura do `Perfil` subiu para
  antes do laço, porque ela é o fallback da fonte que não declarou alíquota.
- `backend/tests/test_caixa.py` — 22 testes novos (64 → 86 no arquivo): base e posição do
  compromisso, precedência do
  imposto por fonte, regressão campo a campo, o piso legal do percentual, a apuração por fonte
  pela leitura (com banco) e a entrada de `evento_previsivel` na varredura de exclusão.
- `docs/features/F-011-renda-tipada/reports/T1.md` — este relatório.

Validation executed:

```text
BASELINE (20/08/2026, antes da mudança)
  cd backend && venv/bin/pytest → 620 passed, 23 warnings
  Nenhuma falha preexistente. Avisos conhecidos: HTTP_422_UNPROCESSABLE_ENTITY depreciado,
  Starlette/httpx e InsecureKeyLength do JWT de teste.

FINAL
  cd backend && venv/bin/pytest → 659 passed, 23 warnings
  (SQLite, o default da suíte. Mesmos 23 avisos do baseline: nenhum é novo.)

  A diferença de 39 testes NÃO é toda desta tarefa: 22 são dela (test_caixa.py, 64 → 86) e
  17 chegaram no commit 977a00e, de F-012 T1 (`backend/tests/test_script.py`), que entrou no
  worktree durante esta execução. O baseline de 620 é anterior aos dois.

  DEVONADA_TEST_DATABASE_URL=postgresql+psycopg://…/devonada_test venv/bin/pytest
    → 659 passed, 23 warnings
  (Postgres 5433, executado porque a tabela e as colunas novas são justamente o que o
  SQLite não confere.)

  venv/bin/alembic upgrade head    → 116f2181bdda -> 482c266f5c6a
  venv/bin/alembic downgrade -1    → 482c266f5c6a -> 116f2181bdda
  venv/bin/alembic upgrade head    → 116f2181bdda -> 482c266f5c6a
  venv/bin/alembic current         → 482c266f5c6a (head), cabeça única
  (Contra o Postgres local, que estava em `116f2181bdda` antes — banco com dado, não vazio.)

  T1-AC7 — conferência manual do DDL contra `CreateTable(Base.metadata)`:
    `venv/bin/alembic check` não detecta NENHUMA operação pendente em `perfil`,
    `fonte_renda` ou `evento_previsivel`. O único desvio que ele acusa é preexistente e de
    área não tocada (ver Remaining risks).
    `CreateTable` rendido × `\d evento_previsivel` do Postgres: idênticos coluna a coluna —
    tipos, nulidade, `server_default now()`, PK e o índice `ix_evento_previsivel_tenant_id`.
    As três colunas aditivas: `integer`, `is_nullable = YES` nas três.
```

Validation skipped:

- `npm run typecheck`, `lint`, `test`, `bundle:check`, `palette:check`, `digits:check`. Nenhum
  arquivo de `src/` foi tocado por esta tarefa, e a feature F-012 executa em paralelo NESTE
  worktree — um portão de front executado agora mediria o trabalho em curso de outra tarefa, não
  o desta. O baseline de front declarado para a onda (45 suítes / 539 testes verdes) permanece o
  registrado pelo orquestrador.

Unavailable capabilities: none.

Assumptions:

- `evento_previsivel.mes_previsto` é `Integer` de 1 a 12, no molde de
  `provisao_anual.mes_vencimento`, e não `AAAA-MM` como `recebimento.mes`. O contrato diz
  "mes_previsto" sem fixar o tipo; o evento se repete todo ano, e `AAAA-MM` obrigaria a
  recadastrar dezembro em janeiro.
- `evento_previsivel.valor` e `tipo` nascem `NOT NULL`; só `fonte_id` é opcional, que é o único
  campo que o contrato declara opcional.
- O `Perfil` passou a ser lido ANTES do laço das fontes (era lido depois). É a mesma query, uma
  só vez; ela subiu porque é o fallback da alíquota de cada fonte.
- Os testes que tocam banco (`TestAliquotaPorFonteNaLeitura`,
  `TestEventoPrevisivelEntraSozinhoNaExclusao`) foram escritos em `backend/tests/test_caixa.py`
  porque ele é o único arquivo de teste no escopo de escrita declarado para T1 — ver
  Human decisions required.

Remaining risks:

- **Desvio preexistente de schema, em área não tocada:** `venv/bin/alembic check` acusa
  `Detected removed unique constraint 'uq_fechamento_mes_tenant_mes' on 'fechamento_mes'`. A
  constraint é criada pela migração `b3c17d2e9a04` (M7.1) e nunca foi declarada em
  `orm.FechamentoMes`, então o banco a tem e o metadata não. É anterior a esta tarefa e não foi
  corrigida aqui — `fechamento_mes` está fora do escopo de T1.
- `caixa_snapshot` NÃO ganhou coluna para o compromisso percentual. O M11 deu uma ao respiro
  justamente para explicar, meses depois, sobre qual cascata um acordo foi proposto; o snapshot
  vai registrar uma `capacidade_maxima` já derrubada pelo compromisso sem registrar a linha que a
  derrubou. Não está no escopo de T1 — fica nomeado para T3 ou T6.
- `docs/backend.md`, na tabela "As regras financeiras e suas fontes", ainda não tem as linhas do
  compromisso percentual (dado do usuário) nem de `percentualInvadeOPiso` (Decreto 11.150/2022,
  art. 3º, na redação do Decreto 11.567/2023), que são as gêmeas exatas das duas linhas do
  respiro. `docs/backend.md` não aparece no escopo de nenhuma das seis tarefas do plano — T6
  lista `api-contract`, `design-system`, `domain`, `roadmap` e `inventario`, mas não este.
- `FonteRenda.variavel` continua inerte (PF-6 do plano). Não foi tocada, como o contrato manda.
- `_preenchimento` não olha para os campos novos: quem declarar SÓ o compromisso percentual
  continua classificado como `nivel_0`. O contrato não pede a mudança e mexer nela alteraria o
  `preenchimento` de quem já usa o app.

Human decisions required:

- **Onde os testes com banco de T1 devem morar.** `T1-AC4` (somatório por fonte com fallback no
  `Perfil`) e `T1-AC6` (a tabela nova na varredura de exclusão) só se provam tocando o banco, e o
  escopo de escrita de T1 lista um único arquivo de teste, `backend/tests/test_caixa.py` — cujo
  cabeçalho diz "Sem banco: o motor é puro". A casa natural desses testes é
  `backend/tests/test_caixa_api.py` (onde vivem os testes de `montar_entrada_caixa`) e
  `backend/tests/test_conta.py` (onde vive `TestNenhumaTabelaFicaDeFora`). Escrevi em
  `test_caixa.py`, sob um separador que declara por quê, para não escrever fora do escopo
  autorizado. Mover é decisão de quem revisa; se aprovada, é uma movimentação de bloco, sem
  mudança de asserção.
- A migração e sua estratégia para o dado de `tipo` já existente em produção continuam sendo gate
  humano do contrato. A estratégia executada é a do plano: nenhum dado migra, todas as colunas
  nascem `nullable`, e quem não declarar nada tem o número de hoje campo a campo.
